# Mod(le)s 🤖

A faster, single-page alternative to [models.dev](https://models.dev), built on top of their awesome [open-source model data](https://github.com/nicepkg/models.dev).

## Features

- ⚡ **Instant search** — fuzzy filter by model name, provider, family or ID
- 📊 **Sortable columns** — sort by price, context size, capabilities and more
- 🔍 **Side-by-side comparison** — select 2–4 models and compare them with best-value highlighting
- 📋 **One-click copy** — copy model IDs or full model specs to clipboard
- 🌗 **Light / Dark theme** — respects system preference, toggleable
- 🖥️ **Virtualized table** — handles thousands of models without breaking a sweat
- 📱 **Responsive** — works on desktop and mobile

## Usage

Open `index.html` in your browser. That's it — no build step, no dependencies.
